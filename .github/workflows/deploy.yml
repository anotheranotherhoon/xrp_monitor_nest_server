name: Deploy to Oracle Cloud

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to VM
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.OCI_HOST }}
          username: ${{ secrets.OCI_USERNAME }}
          key: ${{ secrets.OCI_SSH_KEY }}
          script: |
            cd ~/app
            git pull origin master
            docker-compose down
            export CRYPTO_COMPARE='${{ secrets.CRYPTO_COMPARE }}'
            export OCI_OBJECT_STORAGE_NAMESPACE='${{ secrets.OCI_OBJECT_STORAGE_NAMESPACE }}'
            export OCI_OBJECT_STORAGE_BUCKET='${{ secrets.OCI_OBJECT_STORAGE_BUCKET }}'
            docker-compose up -d --build
